[project]
name = "sbterm"
version = "0.1.0"
description = "Simple terminal for experimenting with SensorBus"
readme = "README.md"
requires-python = ">=3.11"
dependencies = []

[project.optional-dependencies]
pi = [
    "RPi.GPIO; sys_platform == 'linux' and platform_machine == 'armv7l'"
]
